Attribute VB_Name = "Aula2"
Option Explicit

Sub sbLepasta()
    Dim sNomeArquivo
    
    sNomeAquivo = Dir(Range("B1") & "*.xls*", vbDirectory)
    ragne("B2") = sNomeArquivo
    'Msg Box GetAttr(range()"B1") & Range("B3"))

    
 

End Sub
